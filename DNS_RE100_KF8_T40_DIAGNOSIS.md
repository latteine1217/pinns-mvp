# Re=100 k_f=8 DNS 模擬診斷報告

## ❌ 模擬狀態：部分成功（t=0-22.9s 有效，t≥22.9s 數值崩潰）

**執行時間**：2025-11-22  
**配置文件**：Re=100, k_f=8, T_end=40s, 擾動@t=5s  
**後端**：PyTorch MPS (Apple Metal GPU)  
**結果**：數值在 t≈22.9s 出現 NaN，僅 57.2% 數據有效

---

## 📊 執行摘要

### 配置參數

| 參數 | 數值 | 說明 |
|------|------|------|
| **雷諾數 (Re)** | 100 | 完全湍流區域（Re ≥ 60） |
| **強迫波數 (k_f)** | 8 | 8 條橫向條紋 |
| **黏滯係數 (ν)** | 0.004419 | Re = A / (ν² × k_f³) |
| **網格大小 (N)** | 512×512 | 26萬個網格點 |
| **時間步長 (dt)** | 0.001 | CFL 條件自動計算 |
| **擾動時刻** | t=5.0s | unstable_mode 方法 |
| **目標時長** | 40.0s | 實際僅達 22.9s |

### 數據統計

| 指標 | 數值 |
|------|------|
| **總 frames** | 801 |
| **有效 frames** | 458 (57.2%) |
| **有效時間範圍** | t=0 → 22.85s |
| **NaN 開始時刻** | t≈22.90s |
| **總耗時** | 465.4 秒 (7.8 分鐘) |
| **平均速度** | 85.9 steps/s |

---

## ✅ 成功部分：擾動激發與湍流演化（t=0-22.9s）

### 動能演化

| 階段 | 時刻 | 動能 (KE) | 說明 |
|------|------|----------|------|
| **初始** | t=0 | 0.000 | 零初始條件 |
| **擾動前** | t=5.0 | 1.80 | 層流背景建立 |
| **峰值** | t=10.55 | **2.82** | 擾動後湍流爆發 |
| **衰退中** | t=15.0 | 0.69 | 快速衰退階段 |
| **回升** | t=22.85 | 2.06 | 再次激發（異常） |
| **崩潰** | t≈23.0 | **NaN** | 數值不穩定 |

**關鍵觀察**：
- ✅ 擾動成功激發湍流（KE: 1.80 → 2.82）
- ⚠️ t=10-15s 快速衰退（KE: 2.82 → 0.69，下降 75%）
- ❌ t=20-23s **異常回升**（KE: 1.09 → 2.06，增長 88%）→ 數值不穩定

### 渦強度演化

| 階段 | 渦強度 (Enstrophy) | 變化 |
|------|-------------------|------|
| **初始** | 0.000e+00 | - |
| **擾動時刻 (t=5.0)** | 2.74e-05 | 激發 10¹⁰ 倍 |
| **峰值 (t=22.85)** | 6.34e-03 | 持續增長 |

**關鍵觀察**：
- ✅ 擾動立即激發渦度
- ⚠️ 渦強度在 t=20-23s **劇烈增長**（1.21e-03 → 6.34e-03，增長 5.2×）

---

## ❌ 失敗原因：數值不穩定（t≈23s）

### NaN 前兆（t=20-23s）

| 指標 | t=20s | t=22.85s | 變化 |
|------|-------|----------|------|
| **動能 (KE)** | 1.094 | 2.060 | **+88.3%** ⚠️ |
| **渦強度** | 1.207e-03 | 6.343e-03 | **+426%** ⚠️ |
| **散度誤差** | 1.939 | **10.712** | **+452%** 🚨 |

### 根本原因分析

#### 1. **散度誤差爆炸** 🚨

**散度自由條件**：∇·u = 0（不可壓縮流體）

**實際情況**：
- t=0-15s：散度誤差 < 2.0（可接受）
- t=18s：散度誤差 = 2.18（開始不穩定）
- t=20.5s：散度誤差 = 2.35（持續惡化）
- **t=22.85s：散度誤差 = 10.71** 🚨（崩潰前兆）
- t=23.0s：**NaN**（數值爆炸）

**診斷**：
- Pseudo-spectral 方法的去混疊（2/3 規則）**可能失效**
- 高波數（k_f=8）導致數值耗散不足
- PyTorch MPS 的 float32 精度累積誤差

#### 2. **能量異常回升** ⚠️

正常的 Re=100 湍流應該：
- 擾動後達到峰值
- 逐漸衰退至穩定值（10-20% 峰值）

**實際觀察**：
- t=10.55s：峰值 KE=2.82
- t=15.0s：衰退至 KE=0.69（正常）
- **t=20-23s：回升至 KE=2.06**（異常）

**可能原因**：
- 數值誤差累積導致「虛假能量注入」
- 散度條件違反導致壓力場不準確
- k_f=8 的高頻強迫與數值格式不匹配

#### 3. **PyTorch MPS 精度限制**

**已知問題**：
- MPS 使用 **float32**（單精度）
- NumPy 默認 **float64**（雙精度）
- 散度誤差：MPS ~10⁻²，NumPy ~10⁻¹⁶

**累積效應**：
- 40,000 步積分 × float32 誤差 = 顯著累積
- 在 t≈23s（23,000 步）達到臨界點

---

## 🔍 與 Re=99 k_f=4 對比

| 指標 | Re=99 k_f=4 (CPU) | Re=100 k_f=8 (GPU) | 差異 |
|------|------------------|-------------------|------|
| **峰值動能** | 21.80 @ t=5.0 | 2.82 @ t=10.55 | **-87%** |
| **最終動能** | 1.67 | 2.06 @ t=22.85 | +23% |
| **維持率** | 7.7% | 73.1% | **+65%** |
| **數值穩定性** | ✅ 完成 40s | ❌ 崩潰 @ 22.9s | 失敗 |
| **散度誤差** | ~10⁻¹⁶ | **10.7** → NaN | 惡化 |

**關鍵差異**：
1. **k_f=8 峰值動能遠低於 k_f=4**（2.82 vs 21.80）
2. **k_f=8 湍流維持率異常高**（73.1% vs 7.7%）→ 可能是數值誤差而非真實物理
3. **GPU (MPS) 在 k_f=8 時數值不穩定**

---

## 🎯 結論與建議

### 主要發現

1. ✅ **擾動機制有效**：成功在 t=5s 激發湍流
2. ⚠️ **k_f=8 峰值異常低**：相比 k_f=4 下降 87%
3. ❌ **GPU 數值不穩定**：在 t≈23s 散度誤差爆炸導致 NaN

### 失敗原因總結

| 原因 | 貢獻度 | 說明 |
|------|--------|------|
| **高波數 (k_f=8)** | 🔴🔴🔴 | 需要更精細網格或更小時間步長 |
| **PyTorch MPS 精度** | 🔴🔴 | float32 累積誤差 |
| **長時間積分** | 🔴 | 23,000 步後誤差累積顯著 |

### 修正方案

#### 方案 A：降低數值難度（推薦） ⭐

```bash
# 1. 減小時間步長（提升穩定性）
python scripts/generate_kolmogorov_dns.py \
  --N 512 --nu 0.004419 --k_f 8 --dt 0.0005 \  # dt 減半
  --T_end 40.0 --perturbation_times 5.0 \
  --output data/kolmogorov_dns_re100_kf8_t40_dt0005.h5

# 2. 增加網格解析度（更精細）
python scripts/generate_kolmogorov_dns.py \
  --N 1024 --nu 0.004419 --k_f 8 \  # 網格加倍
  --T_end 40.0 --perturbation_times 5.0 \
  --output data/kolmogorov_dns_re100_kf8_t40_N1024.h5
```

#### 方案 B：切換後端（提升精度）

```bash
# 使用 NumPy (float64)
python scripts/generate_kolmogorov_dns.py \
  --N 512 --nu 0.004419 --k_f 8 \
  --T_end 40.0 --perturbation_times 5.0 \
  --backend numpy \  # 強制使用 NumPy
  --output data/kolmogorov_dns_re100_kf8_t40_numpy.h5
```

#### 方案 C：降低物理難度

```bash
# 回到 k_f=4（已驗證穩定）
python scripts/generate_kolmogorov_dns.py \
  --N 512 --nu 0.01257 --k_f 4 \  # Re=99, k_f=4
  --T_end 40.0 --perturbation_times 5.0 \
  --output data/kolmogorov_dns_re99_kf4_t40.h5
```

---

## 📊 輸出文件

### 數據文件

- **HDF5**: `data/kolmogorov_dns_re100_kf8_t40.h5` (部分有效，t=0-22.9s)
- **日誌**: `log/dns_re100_kf8_t40.log`
- **診斷圖**: `results/dns_re100_kf8_t40_partial_analysis.png`

### 使用有效數據

```python
import h5py
import numpy as np

with h5py.File('data/kolmogorov_dns_re100_kf8_t40.h5', 'r') as f:
    ke = f['diagnostics']['kinetic_energy'][:]
    valid_idx = ~np.isnan(ke)  # 過濾 NaN
    
    # 使用 t=0-22.9s 的有效數據
    ke_valid = ke[valid_idx]
    # ...
```

---

## 📝 教訓與經驗

### 數值模擬最佳實踐

1. **高波數需更精細網格**：k_f=8 需要 N≥1024
2. **GPU 精度限制**：長時間積分優先使用 NumPy (float64)
3. **散度誤差監控**：設置閾值（>1.0 則警告）
4. **檢查點保存**：定期保存有效數據，避免全損失

### k_f 選擇建議

| k_f | 網格 (N) | 精度要求 | 穩定性 | 推薦場景 |
|-----|---------|---------|--------|---------|
| **4** | 512 | float32 OK | ✅ 高 | 快速驗證、Re 掃描 |
| **8** | 1024+ | float64 建議 | ⚠️ 中 | 高解析度研究 |
| **16** | 2048+ | float64 必需 | ❌ 低 | 精細結構分析 |

---

**報告完成時間**：2025-11-22  
**下一步行動**：執行方案 A（減小 dt）或方案 B（NumPy 後端）重新模擬
