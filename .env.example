# ========================================
# PINNs MVP 環境變數配置範例
# ========================================
#
# 使用方法:
# 1. 複製此檔案為 `.env`: cp .env.example .env
# 2. 填入實際的 token 和路徑
# 3. 確保 `.env` 已加入 .gitignore (避免洩漏憑證)

# ========================================
# JHTDB API 認證
# ========================================
# 
# 如何申請 JHTDB auth token:
# 1. 訪問: http://turbulence.pha.jhu.edu/webquery/auth.aspx
# 2. 填寫申請表單（需提供機構郵箱）
# 3. 獲取格式為 "institution.email-xxxxxxxx" 的 token
# 4. 將 token 填入下方（移除引號）
#
# 注意: 
# - Mock 模式不需要 token（use_mock=True）
# - HTTP 客戶端會優先使用此 token，若無效則自動降級為 Mock 模式
JHTDB_AUTH_TOKEN=com.gmail.felix.tc.tw-be0cb22a

# ========================================
# 資料路徑配置（可選）
# ========================================
# 
# 若需自訂快取或資料目錄，取消註解並修改路徑
# JHTDB_CACHE_DIR=data/jhtdb
# LOWFI_DATA_DIR=data/lowfi
# CHECKPOINT_DIR=checkpoints
# RESULTS_DIR=results
# LOG_DIR=log

# ========================================
# 訓練配置（可選）
# ========================================
#
# 用於覆蓋配置檔預設值
# CUDA_VISIBLE_DEVICES=0  # 指定 GPU 編號
# OMP_NUM_THREADS=8       # CPU 並行執行緒數
# PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:512  # GPU 記憶體分配策略
